<div class="d-flex justify-content-between flex-wrap flex-md nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
  <h1 class="h2">Editing: <%= user.nome %></h1>
  <div class="btn-toolbar mb-2 mb-md 0">
    <div class="btn-group me-2">
      <a href="/addAdmin" class="btn btn-sm btn-outline-secondary">+ Novo Usuário</a>
    </div>
  </div>
</div>

<div class="col py-3">
  <div class="row">
    <div class="col">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="/paineldecontrol">Painel de Controle</a></li>
          <li class="breadcrumb-item active"><%= user.nome %></li>
        </ol>
      </nav>
    </div>
    <div class="col text-end fw-lighter">
      <b>Last Updated: <%= new Date(user.data_atualizacao).toUTCString()  %></b>
      <b>UserId:</b> <%= user._id %>
    </div>
  </div>

</div>


<form action="/edit/<%= user._id %>?_method=PUT" method="POST">
  <div class="row form-group mb-4">
    <div class="col">
        <label for="nome">Nome</label>
        <input type="text" class="form-control" id="nome" name="nome" value="<%= user.nome %>" placeholder="Nome" required>
    </div>
    <div class="col">
        <label for="email">E-mail</label>
        <input type="text" class="form-control" id="email" name="email" value="<%= user.email %>" placeholder="email@email.com" required>
    </div>
    <div class="col">
        <label for="senha">Senha</label>
        <input type="text" class="form-control" id="senha" name="senha" value="<%= user.senha %>" placeholder="" required>
    </div>
    <div class="col">
        <label for="ddd">DDD</label>
        <input type="number" class="form-control" id="ddd" name="ddd" value="<%= user.ddd %>" placeholder="00">
    </div>
    <div class="col">
        <label for="numero">Número</label>
        <input type="number" class="form-control" id="numero" name="numero" value="<%= user.numero %>" placeholder="123456789">
    </div>
    <div class="form-group mb-4">
      <button type="" class="btn btn-primary">Atualizar Usuário</button>
      <button type="button" class="btn btn-danger" data-bs-toggle="modal" id="deleteButton" data-bs-target="#deleteModal">Deletar Usuário</button>
    </div>
</div>

</form>
<div class="modal fade" tabindex="-1" role="dialog" id="deleteModal">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <div class="modal-title">Você está prestes a remover um registro de Usuário..</div>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p>
          Isso removerá o registro do usuário de <b class="fw-bold"><%= user.nome %></b><br/>
          Você tem certeza?
        </p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <form action="/edit/<%= user._id %>?_method=DELETE" method="POST" class="position-relative">
          <button type="submit" class="btn btn-primary">Sim, Apagar Usuário</button>
        </form>
      </div>
    </div>
  </div>
</div>